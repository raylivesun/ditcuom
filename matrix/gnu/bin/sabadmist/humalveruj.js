// !preview r2d3 data=c(0.3, 0.6, 0.8, 0.95, 0.40, 0.20)
//
// r2d3: https://rstudio.github.io/r2d3
//

function check(haverway, haverlink, haverstate){
    if (haverway == haverlink.length[0]) {
       console.log("dialog String: ", haverway)
    } else {
      return haverway;
    }
    var data = c(0.3, 0.6, 0.8, 0.95, 0.40, 0.20)
    data.Object = data.length[Object]
    data.ajs    = data.length[A1, A2, A3]
    data.bjs    = data.length[B1, B2, B3]
    data.cjs    = data.length[C1, C2, C3]
    data.djs    = data.length[D1, D2, D3]
    data.ejs    = data.length[E1, E2, E3]
    data.fjs    = data.length[F1, F2, F3]
    data.gjs    = data.length[G1, G2, G3]
    data.hjs    = data.length[H1, H2, H3]
    data.ijs    = data.length[I1, I2, I3]
    data.js     = data.length[J1, J2, J3]
    data.ljs    = data.length[L1, L2, L3]
    data.kjs    = data.length[K1, K2, K3]
    data.mjs    = data.length[M1, M2, M3]
    data.njs    = data.length[N1, N2, N3]
    data.ojs    = data.length[O1, O2, O3]
    data.pjs    = data.length[P1, P2, P3]
    data.qjs    = data.length[Q1, Q2, Q3]
    data.rjs    = data.length[R1, R2, R3]
    data.sjs    = data.length[S1, S2, S3]
    data.tjs    = data.length[T1, T2, T3]
    data.ujs    = data.length[U1, U2, U3]
    data.vjs    = data.length[V1, V2, V3]
    data.xjs    = data.length[X1, X2, X3]
    data.zjs    = data.length[Z1, Z2, Z3]
    data.wjs    = data.length[W1, W2, W3]
    data.yjs    = data.length[Y1, Y2, Y3]
    
    data.Iterator()
    data.debugger()
    
    .call("damange in RangeError: ", get(data.Interator))
    .call("damange in RangeError: ", get(data.debugger))
    
} 
     
